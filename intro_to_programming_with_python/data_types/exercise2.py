# 2. Create a tuple called names that contains several pet names. 

print('Creating a tuple of pet names\n')

pet_names = (
    'Blue',
    'Bailey',
    'Tiger Lily',
)

print(pet_names)

print(type(pet_names))